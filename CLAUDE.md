# 오시에테 (おしえて)

협업 성향을 검사하고 같이 일할 사람들과의 궁합을 보는 웹 서비스입니다. 한 사람이 검사를 마치면 공유 URL이 생기고, 그 링크로 팀원이 들어올 때마다 팀 결과가 갱신됩니다.

## 지금 구조

빌드 도구가 없습니다. `prototype/index.html` **한 파일이 전체 서비스**이고 그 파일이 원본입니다. 다른 파일에서 생성하는 구조가 아니니 직접 고칩니다. 폰트는 jsDelivr CDN 링크로 걸려 있습니다.

| 경로 | 무엇 |
|---|---|
| `prototype/index.html` | 랜딩 · 캐릭터 선택 · 30문항 · 개인 결과. 이게 서비스 전부입니다 |
| `prototype/fonts.html` | 한국어 제목 글꼴 비교용. 아직 안 정했습니다 |
| `prototype/sprites/` | 캐릭터 PNG 두는 곳. 넣으면 코드 격자 대신 그 그림을 씁니다 |
| `docs/spec.md` | 축 정의, 채점, 궁합 규칙, 결과 문구 조립 방식 |
| `docs/questions.md` | 문항 30개 원본. **문체를 고칠 때는 이 파일이 기준입니다** |

`vercel.json` 에서 `prototype` 을 출력 폴더로 지정해 정적 배포합니다. 주소는 <https://oshiete-test.vercel.app> 입니다.

## 검사 로직

- 축 다섯 개. 각 축 6문항, 문항당 0~3점이라 축별 0~18점입니다
- 구간은 셋입니다. 0~5 낮음 / 6~12 중간 / 13~18 높음. 궁합은 원점수가 아니라 구간으로 판정합니다
- 답은 두 문장 × 두 단계(확실히·조금)입니다. 화면에서는 구슬 넷입니다
- 문항은 축을 섞어서 냅니다. 순서는 `ROUND_ORDER`에 있습니다
- 결과 문구는 `COPY`(축별 구간 서술), `COMBO`(축 조합 규칙), 조언 세 단으로 조립합니다
- 결과는 URL 해시에 실립니다. `#s=<5글자>&c=<캐릭터>&n=<별명>`

## 바꾸면 안 되는 것

이유가 있어서 정해진 것들입니다. 근거는 `docs/spec.md`에 있습니다.

- **유형 이름은 손잡이, 본체는 서술입니다** (07-30 변경. 원래는 라벨 전면 금지였는데, 공유할 때 "나 이거 나왔어"라고 말할 것이 필요해서 이름을 도입했습니다). 이름은 축의 극 문구에서 조립합니다 — 가장 치우친 축이 명사, 두 번째 축이 수식어, 총 91가지(두 축 80 + 한 축 10 + 균형 1). 전체 목록은 `docs/type-names.md`. 이름을 위해 서술과 다른 새 개념을 만들지 않습니다. 상세 서술·근거·예외 인용은 그대로 유지합니다
- **결과는 판정이 아니라 규칙 제안입니다.** 궁합 퍼센트를 내지 않습니다. 개수(`손발이 맞는 축 3 / 5`)까지만 씁니다
- **선택지 두 쪽 중 한쪽이 착하거나 성실해 보이면 안 됩니다.** 긍정과 부정으로 읽히는 순간 답이 긍정 쪽으로 쏠립니다
- **중간값을 두지 않습니다.** 1~5 척도로 돌아가면 성향이 아니라 응답 습관이 결과를 갈라 버립니다
- **축을 섞어서 냅니다.** 축별로 묶으면 응답자가 무엇을 재는지 알아차려 몰아서 답합니다
- **축 이름을 화면에 노출하지 않습니다.** [의견이 갈릴 때]처럼 상황 구절로 씁니다
- **결과 문구에 LLM을 쓰지 않습니다.** 같은 답에 같은 결과가 나와야 하고, 응답당 비용이 붙으면 안 되고, 결과가 즉시 떠야 합니다. LLM은 조합 규칙 초안을 뽑을 때만 쓰고 완성된 문장은 코드에 박습니다
- **정답 연출을 넣지 않습니다.** 잘한 답이 있는 것처럼 보이면 답을 꾸미기 시작합니다
- **팀은 방장 포함 최대 네 명입니다**
- **개인 원점수는 본인만 봅니다**

## 화면 규칙

- 라이트 파스텔이 바탕, 비비드는 강조에만 씁니다
- 장식은 전부 픽셀입니다. 무지개와 날개처럼 곡선이 들어가는 건 격자를 손으로 찍지 말고 계산해서 찍습니다
- **쓰지 않는 것**: 알약·캡슐 모양, 카드 나열, 누런톤, 모노스페이스 라벨, 각진 모서리
- 문항 글꼴은 갈무리11(픽셀), 본문은 프리텐다드, 일본어는 프리텐다드 JP입니다
- 배경색은 다섯 문항마다 바뀝니다. 축이 아니라 진행 위치에만 연동됩니다
- 픽셀 글꼴은 원본 크기 11px의 정수배(22·33px)에서 가장 깨끗합니다

## 글쓰기

문서·주석·화면 문구 전부 이 어투입니다.

- `~합니다` 로 씁니다. 강조는 대괄호 `[이렇게]`
- **LLM스러운 표현을 쓰지 않습니다.** 총람·3막·핵심·결정적 같은 화법, 괄호 남용, 세일즈 프레이밍, 가짜 정밀함, 장식 이모지
- 안 된 것은 원인과 함께 적습니다. 왜 그 방법을 버렸는지가 남아야 합니다
- 낯선 영어·업계 용어에는 짧은 풀이를 붙입니다

## 다음에 할 일

1. **팀 결과 화면.** 개인 결과에서 초대 링크를 만드는 흐름입니다. 사람 수에 따라 내용이 달라야 합니다
   - 혼자 — 자리 넷과 초대 링크만. 판정 칸도, 잠긴 칸도 만들지 않습니다
   - 둘 — 쌍 규칙으로 축 다섯 개를 다 냅니다
   - 셋·넷 — 쌍 규칙을 버리고 축 규칙으로 갑니다. 짝이 여섯 쌍이라 다 보여주면 안 읽히고, 서로의 점수가 팀에 돌아다니게 됩니다
   - 개인 화면의 축 막대는 팀 화면에 두지 않습니다
   - 구간(높음·중간·낮음)은 팀에 보여줍니다 (07-30 결정). 원점수는 계속 본인만 봅니다
   - **팀 안 구분 키는 이름입니다** (07-30 결정). 같은 이름은 참가 시점에 막고 바꾸게 합니다. 캐릭터는 중복을 허용합니다. 캐릭터를 잠그면 늦게 온 사람이 고를 게 없어 의욕이 떨어집니다
   - **바통 릴레이는 쓰지 않습니다** (07-30 결정). 개인 결과 화면에 [개인 결과 공유]와 [팀 만들기] 버튼 둘을 두고, 팀 만들기를 누르면 팀 하나에 URL 하나가 생깁니다. 누가 언제 들어와도 같은 URL 입니다. 팀 상태가 링크가 아니라 저장소에 있어야 해서 공유 저장소(DB)가 이 시점부터 필요합니다
2. **분석 퀄리티는 유지한 채 이탈률 줄이기.** 상시 목표입니다. 문항 수를 줄이거나 어투를 가볍게 하는 쪽은 쓰지 않습니다. 지금까지 한 것: 게임 창·발판·5문항마다 배경색, 절반 지남 표시, 첫 다섯 문항을 가볍게 재배치
3. **조합 규칙 늘리기.** 현재 20개, 30~40개까지. 실감은 축별 서술이 아니라 축 사이 상호작용에서 나옵니다
4. **축 1·2 규칙 검증.** 초기 응답이 축 3·4·5만 다뤄서 이 둘은 아직 검증이 안 됐습니다
5. **캐릭터 PNG 교체.** 조건은 `prototype/sprites/README.md`에 있습니다
6. **Next.js + DB로 이전.** 지금은 결과를 URL에 싣고 있어서 팀이 커지면 마지막에 들어온 사람만 전체를 봅니다
